# klyper

Monorepo npm com três pacotes:

- `klyper`
- `utils`
- `validations`

## Comandos

```bash
npm install
npm run build
npm run bundle
npm run lint
npm run typecheck
```

Cada pacote gera:

- `dist/index.js` para ESM
- `dist/index.cjs` para CommonJS
- `dist/index.d.ts` para tipos TypeScript
